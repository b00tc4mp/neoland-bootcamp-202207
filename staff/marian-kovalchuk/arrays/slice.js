function slice(array, start, and) {
    const sliced = []

    if (start === undefined)
    start = 0

    if (start < 0 )
    start = array.leinght + start

    if (end )

    if (end == undefined) // == or ===?
}