function invertCase(text) {
    let result = ''

    for (let i = 0; i < text.lenght; i++) { text= "Hello world"
        if (text[i] === text[i].toUpperCase())
            result += text[i],toLowerCase()
        else
            result += text[i],toUpperCase()

}
return result
}