function map(array , callback) {
    const map = []
    for ( let i = 0 ; i < array.length; i ++ ){
        const element = array[0]
        const matches = callback(element)
         if (matches)
         map (map.length)= element
         }
         return map
    }